<%- include("partials/head", { title }) %>
<%- include("partials/header") %>

<main class="shell page">
  <section class="surface empty-state">
    <span class="eyebrow">404</span>
    <h1><%= title %></h1>
    <p>La ruta solicitada no existe o el recurso ya no esta disponible.</p>
    <a class="button button--primary" href="<%= currentUser ? (currentUser.role === 'admin' ? '/admin/reservations' : '/spaces') : '/login' %>">Ir a una vista válida</a>
  </section>
</main>

<%- include("partials/footer") %>
